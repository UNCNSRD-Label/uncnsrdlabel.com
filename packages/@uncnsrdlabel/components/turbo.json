{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "env": ["NEXT_PUBLIC_SITE_NAME"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "outputs": ["dist/**"]
    }
  }
}
