{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "env": [],
      "outputs": ["dist/**"]
    },
    "dev": {
      "outputs": ["dist/**"]
    }
  }
}
