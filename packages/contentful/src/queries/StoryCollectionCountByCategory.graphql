#import "../fragments/StoryCollectionBasicInformation.graphql"

query StoryCollectionCountByCategory(
  $categorySlug: String!
  $locale: String
  $preview: Boolean
) {
  storyCollection(where: { category: { slug: $categorySlug } }) {
    total
  }
}
