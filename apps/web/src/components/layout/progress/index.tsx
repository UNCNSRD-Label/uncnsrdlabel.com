export function Progress() {
  return (
    <i className="hidden supports-[animation-timeline:view()]:block animate-grow-progress from-hotOrange to-hotPink fixed inset-x-0 top-0 h-0.5 origin-left bg-gradient-to-r from-30% to-70% [animation-timeline:_scroll()]" />
  );
}
