export function Progress() {
  return (
    <i className="animate-grow-progress from-hotOrange to-hotPink fixed inset-x-0 top-0 hidden h-0.5 origin-left bg-gradient-to-r from-30% to-70% [animation-timeline:_scroll()] supports-[animation-timeline:view()]:block" />
  );
}
